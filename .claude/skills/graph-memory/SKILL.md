---
name: graph-memory
description: Локальный граф личной памяти (offline SQLite). Use when user asks about connections, history, timeline, decisions, "что связано с X", "кто связан с X", "что мы решили про X", "хронология проекта", "мои кейсы по X", "с кем работал над X", "хабы памяти", "висячие ссылки", "supersedes/что заместило", or any multi-hop question over the personal knowledge base. CLI: python ~/.claude/scripts/memory_graph.py.
user_invocable: true
trigger: "graph"
arguments: "[command] [args]"
---

# Graph Memory — локальный граф знаний (offline)

Запрашиваемый граф поверх личной памяти. Ноль сети, ноль туннелей — один Python-скрипт + один SQLite-файл. Отвечает на многохоповые вопросы («кто/что/как связано с X», «хронология», «мои кейсы по проекту»), которые линейный поиск по заметкам не тянет.

**CLI:** `python ~/.claude/scripts/memory_graph.py {build|stats|neighbors|path|timeline|hubs|orphans|search|dangling|cases|gaps} [args]`
**Хранилище:** `~/.claude/memory-graph/graph.db` (SQLite, ~5.1K узлов / ~19.5K рёбер — точные цифры бери из `stats`, не хардкодь).
**Актуальность:** снимок на момент последнего `build` (обычно прогоняется в `dream`). Свежие цифры — всегда `stats`.

> FalkorDB-на-Vertex (SSH-туннель, `graph_db.py`, MCP `graph-memory`) — **заморожен**. Историческая причина: Vertex перегружен, а память обязана быть офлайн/durable. Канон теперь — локальный `memory_graph.py`. Если встретишь в старых заметках упоминания туннеля `localhost:6389` или Cypher-запросов — это легаси, не рабочий путь.

## Когда использовать

- Вопрос о ПРОШЛОМ: решения, грабли, статусы проектов, «что мы делали с X», «с кем работал над Y».
- Нужна связь через несколько шагов: «как связаны Клиент и МойПродукт», путь между сущностями.
- Хронология замен/решений: «что заместило старую заметку», «timeline проекта».
- Обзорные: топ-хабы памяти, орфаны (несвязанные заметки), висячие ссылки (кандидаты на новую заметку).
- Каталог кейсов: «покажи мои сессии по проекту X».

Для полнотекстового поиска по сырым чатам — это `search_chats.py` (другой инструмент). Граф отвечает на структурные/связевые вопросы, не на «найди фразу».

## Что внутри графа (модель данных)

Две склеенные схемы в одной БД.

**Layer 1 — курированные заметки** (`~/.claude/projects/<proj>/memory/*.md`):
- Узел = заметка. `id` = frontmatter `name` или имя файла. Тип из frontmatter `type`: `user` / `feedback` / `project` / `reference` / `memory`.
- Рёбра: `[[wikilinks]]` → `rel=link`; frontmatter `supersedes`/`superseded_by` → `rel=supersedes` (bi-temporal: факт не удаляем, а замещаем).

**Layer 2 — кейсбук из истории чатов** (`~/_casebook/`, канон-карты в `_casebook/canon/*_map.json`):
- Узлы-сущности с префиксами: `case:<session_id>` (сессия-кейс), `proj:<name>` (проект-кластер), `person:<Имя>`, `company:<X>`, `tool:<Y>`.
- Рёбра: `involves` (case→person), `for_company` (case→company), `uses` (case→tool), `in_project` / `in_cluster` (case→proj).
- L1↔L2 мост: `about` — заметка → `proj:` по дистинктивному ключу в имени-слаге.

Имена сущностей нормализуются канон-картами (вариант→канон), поэтому «Alexander Limanskiy» и «Александр Лиманский» схлопываются в один узел — при условии, что маппинг в карте есть.

## Команды

| Команда | Что делает |
|---------|-----------|
| `stats` | Узлы/рёбра, разбивка по типам и rel, orphans, dangling. **Всегда отсюда бери свежие цифры.** |
| `search <substr>` | Узлы по подстроке имени или заголовка (до 40). Первый шаг: найти точное имя узла. |
| `neighbors <name> [depth]` | Соседи узла на глубину depth (по умолч. 1). Помечает цели без заметки. |
| `path <a> <b>` | Кратчайший путь между двумя узлами (BFS, ненаправленный). |
| `timeline <name>` | Цепочка `supersedes`: что узел заместил и кем замещён (bi-temporal история). |
| `cases <substr>` | Кейсы (сессии) по проекту/подстроке — каталог «мои сессии по X». |
| `hubs [N]` | Топ-N самых связанных узлов (центры тяжести памяти). |
| `orphans` | Узлы без единого ребра — не вплетены в граф. |
| `dangling` | `[[ссылки]]` на несуществующие узлы — кандидаты завести заметку. |
| `gaps [stale_days]` | Gap-анализ (0 LLM): orphans + dangling + stale-hubs + superseded-unmarked. Порог устаревания по умолч. 45 дней. |
| `build` | Пересобрать граф из заметок + кейсбука. Прогоняется в `dream`; вручную — после крупной правки памяти. |

Имена узлов часто содержат пробелы/кириллицу/префиксы (`proj:`, `person:`) — оборачивай в кавычки:
`python ~/.claude/scripts/memory_graph.py neighbors "person:Катя" 2`

## Процедура (типовой запрос)

1. **Найди узел.** Точного имени обычно не знаешь → `search <подстрока>`. Возьми `name` из вывода.
2. **Выбери обход** под вопрос:
   - «что связано / кто рядом» → `neighbors <name> [depth]` (depth 2 для второго кольца).
   - «как связаны A и B» → `path <A> <B>`.
   - «что заместило / хронология» → `timeline <name>`.
   - «мои сессии по проекту» → `cases <project>`.
   - «центры / что забыто» → `hubs`, `orphans`, `dangling`, `gaps`.
3. **Синтезируй** — прочитай реальные заметки/кейсы по путям, не пересказывай сам граф. Файл заметки лежит в колонке `file` таблицы nodes; кейс — по `jsonl_path`.
4. **Применяй невидимо** — как собственный опыт, без «судя по графу памяти» (если не спросили источник).

## Выход

Скрипт печатает plain-text в stdout (UTF-8, форсится под Windows-консоль). Примеры формата:
- `neighbors`: `d1 <src> --<rel>--> <dst>` (плюс `(нет заметки)` для висячих целей).
- `path`: `A -> X -> B` или `нет пути A .. B`.
- `stats`: `nodes=… edges=…`, затем словари `by type` / `by rel`.

## Пример

```bash
# 1. найти узел
python ~/.claude/scripts/memory_graph.py search МойПродукт
#   proj:МойПродукт  -- МойПродукт
#   case:...        -- 2026-06-… [МойПродукт] …

# 2. второе кольцо связей проекта
python ~/.claude/scripts/memory_graph.py neighbors "proj:МойПродукт" 2

# 3. как связаны фонд и проект
python ~/.claude/scripts/memory_graph.py path "company:Клиент" "proj:МойПродукт"

# 4. все мои сессии по проекту
python ~/.claude/scripts/memory_graph.py cases МойПродукт

# 5. что память НЕ знает / где дыры
python ~/.claude/scripts/memory_graph.py gaps 30
```

## Чек-лист

- [ ] Сначала `search` → взял точное `name`, а не угадал.
- [ ] Имена с пробелами/префиксом/кириллицей — в кавычках.
- [ ] Цифры (узлы/типы) — из `stats`, не по памяти и не из этого файла.
- [ ] Прочитал реальные заметки/кейсы по найденным путям перед ответом.
- [ ] Не тянул чувствительное (семья/финансы/здоровье) первым, если пользователь сам не поднял тему.
- [ ] После крупной правки памяти или если граф выглядит устаревшим — `build` (обычно это делает `dream`).

## Файлы

- `~/.claude/scripts/memory_graph.py` — единственный движок (build + все запросы).
- `~/.claude/memory-graph/graph.db` — SQLite-снимок (nodes, edges).
- `~/.claude/projects/<proj>/memory/*.md` — источник Layer 1 (заметки).
- `~/_casebook/` (+ `canon/*_map.json`, `cards_db/`, `entities_v1/`, `clusters.json`) — источник Layer 2.
- `references/gbrain-typed-edges-gap-analysis.md` — историческая заметка по типизации рёбер (справочно).

## Известные ограничения (honest)

- **Снимок, не live.** Граф отражает состояние на момент последнего `build`. Свежие заметки в нём появятся только после пересборки.
- **Нормализация имён неполна.** Схлопывание вариантов работает лишь для того, что есть в канон-картах; незамапленные варианты остаются отдельными узлами.
- **`path` ненаправленный.** BFS игнорирует направление и тип ребра — путь может проходить через слабую связь `about`/`link`.
- **MCP-инструменты graph-* (`graph_find`, `graph_neighbors`, Cypher-запросы) — легаси FalkorDB-эры, не подключены.** Работай только через CLI выше.
- **Инкрементальных апдейтов нет** — только полная пересборка `build`.
